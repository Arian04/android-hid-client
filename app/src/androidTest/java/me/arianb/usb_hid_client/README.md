Description of each subpackage in this directory

- `unit` -> Android unit tests (any unit tests that require the Android framework, even if they don't test the UI)
- `unit_ui` -> Android unit tests that focus on testing a unit of the UI
- `ui` -> full-blown instrumented UI tests that test multiple UI components